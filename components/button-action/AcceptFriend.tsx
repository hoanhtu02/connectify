import { Button } from "@/components/ui/button";

function AcceptFriend({ id }: { id: string }) {
  return (
    <Button variant="outline" size="sm">
      Accept
    </Button>
  );
}

export default AcceptFriend;
